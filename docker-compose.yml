version: '3.8'

services:
  mysql-espacios:
    image: mysql:8.0
    container_name: mysql-espacios
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ""
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_DATABASE: db_gestion
    networks:
      - network-gestion
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root"]
      interval: 10s
      timeout: 5s
      retries: 20

  mysql-incidencias:
    image: mysql:8.0
    container_name: mysql-incidencias
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ""
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_DATABASE: db_gestion_incidencias_dev
    networks:
      - network-gestion
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root"]
      interval: 10s
      timeout: 5s
      retries: 20

  mysql-residencias:
    image: mysql:8.0
    container_name: mysql-residencias
    ports:
      - "3308:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ""
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_DATABASE: db_residencia_dev
    networks:
      - network-gestion
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root"]
      interval: 10s
      timeout: 5s
      retries: 20

  eureka-server:
    build: ../eureka
    container_name: eureka-server
    ports:
      - "8761:8761"
    networks:
      - network-gestion
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8761/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s

  api-gateway:
    build: ../gateway
    container_name: api-gateway
    ports:
      - "8080:8080"
    environment:
      EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE: http://eureka-server:8761/eureka/
      SPRING_PROFILES_ACTIVE: dev
    networks:
      - network-gestion
    depends_on:
      eureka-server:
        condition: service_healthy

  espacios-service:
    build: ../Espacios
    container_name: espacios-service
    environment:
      SPRING_PROFILES_ACTIVE: dev
      EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE: http://eureka-server:8761/eureka/
      DATABASE_HOST: mysql-espacios
    networks:
      - network-gestion
    depends_on:
      mysql-espacios:
        condition: service_healthy
      eureka-server:
        condition: service_healthy

  incidencias-service:
    build: ../Incidencias
    container_name: incidencias-service
    environment:
      SPRING_PROFILES_ACTIVE: dev
      EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE: http://eureka-server:8761/eureka/
      DATABASE_HOST: mysql-incidencias
    networks:
      - network-gestion
    depends_on:
      mysql-incidencias:
        condition: service_healthy
      eureka-server:
        condition: service_healthy

  residencias-service:
    build: ../Residencias
    container_name: residencias-service
    environment:
      SPRING_PROFILES_ACTIVE: dev
      EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE: http://eureka-server:8761/eureka/
      DATABASE_HOST: mysql-residencias
    networks:
      - network-gestion
    depends_on:
      mysql-residencias:
        condition: service_healthy
      eureka-server:
        condition: service_healthy

networks:
  network-gestion:
    driver: bridge
